# PDF to Markdown

> High-fidelity converter that transforms PDF documents into clean, structured Markdown while preserving layout nuances.
> 高保真转换器，将 PDF 文档转化为干净、结构化的 Markdown，并保留排版细节。

## Usage
Add this directory to your `~/.claude/skills/` folder to use it with Claude Code.
